#!/bin/bash

arecord | netcat -vl ${2-3333} &
echo $USERNAME `curl --silent ipecho.net/plain` | netcat ${3-localhost} 8081

echo $1 | nc localhost 8080
ip=$(echo $1 | nc ${3-localhost} 8080)
echo "connecting to $ip"
netcat -v $ip 3333 | aplay &


